[package]
name = "todo-store"
version = "0.1.0"
edition = "2021"
rust-version = "1.85"
description = "待办数据层：零 Tauri 依赖的独立 crate"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
uuid = { version = "1", features = ["v4"] }
thiserror = "2"